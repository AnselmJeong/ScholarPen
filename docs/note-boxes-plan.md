# Note boxes and figure-caption math

- Add an editable Note block with a configurable title (default: 읽는 법), subtle background and left border, rich inline content, and nested body blocks.
- Provide /note insertion and a toolbar action for turning existing explanation paragraphs into notes without losing formulas, citations or formatting.
- Export notes as Quarto note callouts and portable Markdown blockquotes. Restore notes on import, including multi-paragraph bodies, lists, formulas and citations. Keep fenced code examples unchanged.
- Render figure-caption Markdown and KaTeX while preserving the exact editable caption source. Retain TeX backslashes during QMD figure import and export.
- Verify schema/JSON persistence, actual editor rendering and caption editing, export/import round trips, code protection, TypeScript, full tests and release build. Do not rewrite user manuscripts automatically.
